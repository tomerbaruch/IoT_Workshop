@model IEnumerable<iot_pub_website.Models.DeviceRating>
@using iot_pub_website.Models;

@{
    ViewBag.Title = "Top rated pubs";
}

<h2>Top rated pubs</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table" id="pubsTable">
</table>



@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Location)
        </th>
        <th>
            Pub rating
        </th>
    </tr>

@foreach (var item in Model) {
    String style = item.Rating > Constants.CO2_LIMIT ? "Background-color:red" : "";
    <tr style="@style">
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Location)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            @Html.ActionLink("5 days", "tomer", new { id = item.Id })
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
    </tr>
}

</table>*@

<div>
    <table>
        <tr>
            <td>Sort by: </td>
            <td>
                <button id="1" class="link">CO2</button>
            </td>
            <td>
                <button id="2" class="link">Sound</button>
            </td>
            <td>
                <button id="3" class="link">Alcohol</button>
            </td>
        </tr>
    </table>
</div>

<script type="text/javascript" src="https://code.jquery.com/jquery-1.7.1.min.js"></script>
<script src="~/Scripts/Devices.js"></script>
