@model IEnumerable<iot_pub_website.Models.Measurement>
@using iot_pub_website.Models;

@{
    ViewBag.Title = "Index";
}

<script type="text/javascript" src="/Scripts/tomer.js"></script>

<h2>Live measurements from Raspberry pie</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Device_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.time)
        </th>
        <th></th>
    </tr>
    
@foreach (var item in Model) {
    String style = item.value > Constants.CO2_LIMIT ? "Background-color:red" : "";
    <tr style="@style">
        <td id="name">
            @*@Html.DisplayFor(modelItem => item.Device_id)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.time)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>

<div>
    @Html.ActionLink("5 days", "fiveDays", new { id=1 })
</div>
